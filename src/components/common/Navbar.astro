---
import { useTranslations, type SupportedLang } from '@/i18n';
import LanguageSwitcher from './LanguageSwitcher.astro';

interface Props {
  lang: SupportedLang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);
const currentPath = Astro.url.pathname;

const navItems = [
  { href: `/${lang}/`, label: t.nav.home },
  { href: `/${lang}/services/`, label: t.nav.services },
  { href: `/${lang}/about/`, label: t.nav.about },
  { href: `/${lang}/contact/`, label: t.nav.contact }
];
---

<header class="sticky top-0 z-40 w-full glass-nav transition-all duration-300">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-20">
      
      <!-- Brand Logo (Left Wing) -->
      <div class="flex-1 flex items-center justify-start">
        <a href={`/${lang}/`} class="flex items-center gap-3 group shrink-0">
          <div class="w-11 h-11 rounded-[2px] bg-navy-900 border border-navy-800 flex items-center justify-center text-gold-400 group-hover:border-gold-500 group-hover:shadow-md transition-all duration-300">
            <svg xmlns="http://www.w3.org/2000/svg" class="w-6 h-6" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="m16 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z" />
              <path d="m2 16 3-8 3 8c-.87.65-1.92 1-3 1s-2.13-.35-3-1Z" />
              <path d="M7 21h10" />
              <path d="M12 3v18" />
              <path d="M3 7h2c2 0 5-1 7-2 2 1 5 2 7 2h2" />
            </svg>
          </div>
          <div>
            <span class="block text-lg font-bold tracking-wider text-navy-950 uppercase font-sans">
              VANGUARD
            </span>
            <span class="block text-[10px] tracking-widest text-gold-600 uppercase font-bold -mt-1">
              & PARTNERS LEGAL
            </span>
          </div>
        </a>
      </div>

      <!-- Desktop Navigation Links (Centered) -->
      <nav class="hidden md:flex items-center justify-center space-x-1 lg:space-x-2 shrink-0" aria-label="Main Navigation">
        {navItems.map((item) => {
          const isActive = currentPath === item.href || (item.href !== `/${lang}/` && currentPath.startsWith(item.href));
          return (
            <a
              href={item.href}
              class:list={[
                "h-9 px-3.5 lg:px-4 inline-flex items-center justify-center text-sm font-medium rounded-[2px] transition-all duration-200 whitespace-nowrap",
                isActive
                  ? "text-navy-950 bg-slate-100 border border-slate-300 font-semibold"
                  : "text-slate-700 hover:text-navy-950 hover:bg-slate-100/70"
              ]}
              aria-current={isActive ? "page" : undefined}
            >
              {item.label}
            </a>
          );
        })}
      </nav>

      <!-- Right Action Area (Language + CTA) (Balanced Right Wing) -->
      <div class="hidden md:flex flex-1 items-center justify-end gap-3.5">
        <LanguageSwitcher currentLang={lang} />

        <a
          href={`/${lang}/contact/`}
          class="relative w-40 h-10 shrink-0 inline-flex items-center justify-center text-xs font-bold uppercase tracking-wider text-white rounded-[2px] bg-gradient-to-r from-gold-600 via-gold-500 to-gold-600 hover:from-gold-700 hover:to-gold-600 transition-all duration-300 shadow-sm hover:shadow active:scale-95 text-center whitespace-nowrap"
        >
          {t.nav.consultationBtn}
        </a>
      </div>

      <!-- Mobile Hamburger Button -->
      <div class="flex items-center gap-3 md:hidden">
        <LanguageSwitcher currentLang={lang} />

        <button
          id="mobile-menu-toggle"
          type="button"
          class="p-2 rounded-[2px] text-slate-700 hover:text-navy-900 hover:bg-slate-100 focus:outline-none"
          aria-label="Toggle Navigation Menu"
        >
          <svg id="hamburger-icon" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg id="close-icon" class="w-6 h-6 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile Dropdown Menu -->
  <div id="mobile-menu" class="hidden md:hidden px-4 pt-2 pb-6 space-y-2 border-b border-slate-200 bg-white/98 backdrop-blur-2xl shadow-xl">
    {navItems.map((item) => (
      <a
        href={item.href}
        class="block px-4 py-3 rounded-[2px] text-base font-medium text-slate-800 hover:text-navy-900 hover:bg-slate-100"
      >
        {item.label}
      </a>
    ))}
    <div class="pt-3">
      <a
        href={`/${lang}/contact/`}
        class="block w-full text-center py-3 text-sm font-bold uppercase tracking-wider text-white rounded-[2px] bg-gradient-to-r from-gold-600 to-gold-500 shadow-md"
      >
        {t.nav.consultationBtn}
      </a>
    </div>
  </div>
</header>

<script>
  function setupMobileMenu() {
    const toggleBtn = document.getElementById('mobile-menu-toggle');
    const menu = document.getElementById('mobile-menu');
    const hamburger = document.getElementById('hamburger-icon');
    const close = document.getElementById('close-icon');

    if (!toggleBtn || !menu) return;

    toggleBtn.addEventListener('click', () => {
      const isExpanded = !menu.classList.contains('hidden');
      if (isExpanded) {
        menu.classList.add('hidden');
        hamburger?.classList.remove('hidden');
        close?.classList.add('hidden');
      } else {
        menu.classList.remove('hidden');
        hamburger?.classList.add('hidden');
        close?.classList.remove('hidden');
      }
    });
  }

  document.addEventListener('DOMContentLoaded', setupMobileMenu);
  document.addEventListener('astro:page-load', setupMobileMenu);
</script>
